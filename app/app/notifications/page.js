import StubScreen from "@/components/StubScreen";

export const metadata = { title: "Notifications — NutriSalma" };

export default function NotificationsPage() {
  return <StubScreen title="Notifications" />;
}
